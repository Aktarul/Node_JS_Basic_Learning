<h1>{{ title }}</h1>
<h1> Welcome to {{ title }}</h1>
{{# if condition }}
    Condition is met.
{{ else }}
    Condition is not met.
{{/ if }}

{{# each anyArray as |val key|}}
    {{ key }}: {{ val }}
{{/each}}